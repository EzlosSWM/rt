---
import navigation from "@data/navLinks.js";
---

<div class="uppercase flex flex-col space-y-8 md:space-y-0 md:flex-row md:space-x-8">
    {
        navigation.map((nav) => (
            <a class="hover:text-accent-gray font-medium" href={nav.link}>{nav.title}</a>
        ))
    }
</div>
